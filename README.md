# Run Locally
**Prerequisites:**  Node.js

1. Install dependencies:
   `npm install`
2. Run the app locally:
   `npm run dev`

# Run Unit Tests
`npm test`

# Build and Deploy
1. Build:
   Run `npm run build` to build and create the dist folder
2. Validate deployed static page:
   Run `npm run preview`